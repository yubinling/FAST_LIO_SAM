# SAT-SLAM 系统架构与运行说明

本文档说明当前工作空间中的两个 ROS 包、主要代码结构、数据流、配置模式和常用启动方式。

## 1. 工作空间组成

当前源码根目录中包含两个 catkin 包：

```text
FAST_LIO_SAM/
├── FAST_LIO_SAM/       # ROS 包 fast_lio_sam：SAT-SLAM 主系统
├── SAULIO/             # ROS 包 point_lio：本地 SAULIO 前端
├── README.md
└── 系统架构与运行说明.md
```

编译后主要生成两个可执行文件：

```text
devel/lib/fast_lio_sam/fastlio_sam_mapping
devel/lib/point_lio/pointlio_mapping
```

统一编译：

```bash
cd /home/lingyubin/Documents/lyb/livo/sat-slam
catkin_make
source devel/setup.bash
```

## 2. 两个 ROS 包的职责

### 2.1 fast_lio_sam

包路径：

```text
FAST_LIO_SAM/FAST_LIO_SAM
```

该包是 SAT-SLAM 主体，负责：

- FAST-LIO 点面 EKF 前端，或接收 SAULIO 外部里程计。
- 读取离线检测 txt，或订阅实时检测话题 `/detect3d`。
- 多目标数据关联、跟踪和动态目标状态维护。
- 动态点剔除。
- 目标局部高度描述子误关联检测。
- 稳定目标局部点云地图和 ICP 质量门控。
- 自车与稳定动态目标的局部联合因子图。
- 可选的全局 GTSAM/iSAM 后端、GPS 和回环约束。
- 地图、轨迹、目标点云和可视化话题发布。

主要文件：

| 文件 | 作用 |
| --- | --- |
| `src/laserMapping.cpp` | 主节点入口，包含前端切换、检测接入、动态剔除、跟踪和后端调度 |
| `src/preprocess.cpp` | 雷达点云预处理 |
| `src/IMU_Processing.hpp` | FAST-LIO IMU 传播和点云去畸变 |
| `include/use-ikfom.hpp` | FAST-LIO 迭代误差状态 Kalman 滤波 |
| `include/tracked_object.h` | 目标状态和跟踪数据结构 |
| `include/successive_shortest_path.h` | SSP 数据关联 |
| `include/factorgraph_opt.h` | 多目标局部图相关优化结构 |
| `src/Scancontext.cpp` | Scan Context 回环相关逻辑 |
| `config/velodyne_robosense.yaml` | 当前 Robosense/SAT-SLAM 主配置 |
| `detection/` | 本地 LivoxDetection TF/TRT 推理代码 |
| `scripts/` | 实时和离线检测启动脚本 |

### 2.2 point_lio

包路径：

```text
FAST_LIO_SAM/SAULIO
```

该包是复制到当前工作空间内的 SAULIO 前端，负责：

- 订阅雷达点云和 IMU。
- 进行 SAULIO 点、线、面观测构建。
- 执行小更新和每帧 bigupdate。
- 维护 SAULIO 局部地图。
- 发布里程计 `/aft_mapped_to_init`。

主要文件：

| 文件 | 作用 |
| --- | --- |
| `src/laserMapping.cpp` | SAULIO 主节点和数据调度 |
| `src/Estimator_1.cpp` | 点、线、面匹配和观测矩阵构建 |
| `src/IMU_Processing.cpp` | IMU 传播和帧末去畸变 |
| `src/preprocess.cpp` | SAULIO 点云预处理 |
| `src/map_manager.cpp` | 局部地图和特征管理 |
| `include/IKFoM/` | SAULIO 使用的 IKFoM 状态估计 |
| `config/robosense_new.yaml` | 联合运行的 SAULIO 配置 |
| `launch/mapping_robosense_new.launch` | 联合运行启动文件 |

## 3. 总体数据流

### 3.1 FAST-LIO 前端模式

```text
/velodyne_points + /IMU_data
        |
        v
fast_lio_sam 内部 FAST-LIO 前端
        |
        +--> 检测读取/订阅
        +--> 动态点剔除
        +--> 多目标跟踪
        +--> 局部联合图
        +--> 可选全局图
```

该模式只需要运行 `fast_lio_sam`。

### 3.2 SAULIO 前端模式

```text
/velodyne_points + /IMU_data
        |
        v
fast_lio_sam 去畸变、检测和动态点剔除
        |
        v
/sat_slam/saulio_filtered_points
        |
        v
point_lio/SAULIO
        |
        v
/aft_mapped_to_init
        |
        v
fast_lio_sam 跟踪和后端
```

此模式运行两个可执行节点，但两个包都在当前工作空间内，不依赖外部 SAULIO 工程。

注意：

- SAULIO 接收的是 SAT-SLAM 发布的处理后点云，不是原始 `/velodyne_points`。
- 即使当前帧没有动态目标，该点云也已经经过 SAT-SLAM 的预处理和去畸变。
- 联合模式下 SAULIO 的 `point_filter_num` 和 `filter_size_surf` 已放宽，避免重复采样造成观测点过少。

## 4. 前端里程计模式

配置文件：

```text
FAST_LIO_SAM/config/velodyne_robosense.yaml
```

参数：

```yaml
frontend_odom_mode: 0
```

| 值 | 含义 | 需要启动的节点 |
| --- | --- | --- |
| `0` | 使用 fast_lio_sam 内部 FAST-LIO 前端 | 只启动 `mapping_robosense.launch` |
| `1` | 使用 SAULIO 发布的里程计 | 同时启动 fast_lio_sam 和 point_lio |

FAST-LIO 模式：

```bash
roslaunch fast_lio_sam mapping_robosense.launch
```

SAULIO 模式：

```bash
# 终端 1
roslaunch fast_lio_sam mapping_robosense.launch

# 终端 2
roslaunch point_lio mapping_robosense_new.launch
```

## 5. 检测输入模式

检测模式由 `detect_file_path` 和所使用的 launch 共同决定。

### 5.1 离线 txt 模式

配置：

```yaml
detect_file_path: "/absolute/path/to/detectmsg/fast4.txt"
```

启动：

```bash
roslaunch fast_lio_sam mapping_robosense.launch
```

主节点启动时读取整个 txt，不订阅实时 `/detect3d`。

### 5.2 实时 TensorFlow 模式

实时 launch 会把 `detect_file_path` 显式置空，并启动检测脚本：

```bash
roslaunch fast_lio_sam mapping_robosense_realtime_detection.launch \
  detection_backend:=tf
```

数据流：

```text
/velodyne_points -> realtime_detection_limot -> /detect3d -> fast_lio_sam
```

### 5.3 实时 TensorRT 模式

```bash
roslaunch fast_lio_sam mapping_robosense_realtime_detection.launch \
  detection_backend:=trt
```

默认 engine：

```text
FAST_LIO_SAM/detection/livox_detection/model/livoxmodel_fp32.engine
```

实时检测等待时间由 launch 参数控制：

```bash
roslaunch fast_lio_sam mapping_robosense_realtime_detection.launch \
  detection_backend:=trt \
  detection_wait_ms:=500
```

`detection_wait_ms` 只用于等待同时间戳检测结果，不改变 LIO 或检测网络算法。

### 5.4 离线生成 txt

离线脚本同时支持 TensorFlow 和 TensorRT：

```bash
FAST_LIO_SAM/scripts/run_offline_detection_limot.sh \
  --bag /path/to/data.bag \
  --topic /velodyne_points \
  --output /path/to/output.txt \
  --backend tf
```

或：

```bash
FAST_LIO_SAM/scripts/run_offline_detection_limot.sh \
  --bag /path/to/data.bag \
  --topic /velodyne_points \
  --output /path/to/output.txt \
  --backend trt \
  --engine_path FAST_LIO_SAM/detection/livox_detection/model/livoxmodel_fp32.engine
```

检测目录的完整说明见：

```text
FAST_LIO_SAM/detection/README.md
```

## 6. 动态点剔除模式

配置：

```yaml
limot:
  if_dynamic: true
  dynamic_filter_mode: 0
```

| `dynamic_filter_mode` | 行为 |
| --- | --- |
| `0` | 不剔除动态点；检测仍可用于目标跟踪和后端 |
| `1` | 使用当前帧所有检测框，在下采样前从 lidar 系点云中剔除框内点 |
| `2` | 使用上一帧 tracker 预测得到的动态框进行剔除 |

`if_dynamic: false` 时不会加载 txt，也不会订阅 `/detect3d`，上述模式均不生效。

## 7. 误关联检测模式

局部高度描述子二次校验：

```yaml
limot:
  association_descriptor_gate_enable: false
  association_descriptor_cos_threshold: 0.55
  association_descriptor_grid_x: 4
  association_descriptor_grid_y: 4
  association_descriptor_min_points_per_cell: 2
  association_descriptor_min_valid_cells: 3
```

行为：

- `false`：完全使用原 SSP 关联结果。
- `true`：仅对成熟轨迹、且当前和历史描述子都有效的匹配进行余弦相似度校验。
- 描述子无效、点太少或有效格不足时回退到原逻辑。
- 阈值越高越严格，越容易拒绝匹配；出现断轨时可先降到 `0.45`。

## 8. 后端结果模式

配置：

```yaml
limot:
  backendResultMode: 1
```

| 值 | 含义 |
| --- | --- |
| `0` | `local_only`：只使用局部滑窗联合图结果 |
| `1` | `local_plus_global`：局部图优化后继续进入全局 GTSAM/iSAM |

全局反馈相关参数：

```yaml
localGraphFeedbackToGlobal: true
localGraphFeedbackNoise: 10.0
localGraphFeedbackMinObjectFactors: 1
localGraphFeedbackMaxTranslation: 1.0
localGraphFeedbackMaxRotation: 0.3
```

`localGraphFeedbackNoise` 越大，局部图对全局图的约束越弱。

## 9. 稳定目标局部地图和 ICP 因子

主要开关：

```yaml
objectLocalMapEnable: true
objectLocalMapAccumulateForPcd: true
objectLocalMapIcpMap: false
objectLocalMapIcpCheckEnable: true
objectLocalMapBackendFactorEnable: true
```

数据处理顺序：

```text
稳定跟踪目标
  -> 目标局部坐标系点云累计
  -> ICP 质量检查
  -> 通过质量门控
  -> 弱先验因子进入局部联合图
```

常用质量门控参数：

| 参数 | 含义 |
| --- | --- |
| `objectLocalMapIcpCheckMinMapPoints` | 局部地图最少点数 |
| `objectLocalMapIcpCheckMinScanPoints` | 当前目标点云最少点数 |
| `objectLocalMapIcpCheckFitnessThreshold` | ICP fitness 上限 |
| `objectLocalMapIcpCheckMaxAbsX/Y/Z` | 位移修正上限 |
| `objectLocalMapIcpCheckMaxAbsYaw` | 航向修正上限 |
| `objectLocalMapBackendFactorNoise` | 后端弱先验噪声 |

目标地图可发布到：

```text
/limot/object_local_maps
```

退出程序时，满足条件的目标地图保存在：

```text
FAST_LIO_SAM/PCD/object_local_maps
```

## 10. SAULIO 联合运行参数

SAULIO 配置：

```text
SAULIO/config/robosense_new.yaml
SAULIO/launch/mapping_robosense_new.launch
```

关键参数：

```yaml
common:
  lid_topic: "/sat_slam/saulio_filtered_points"
  imu_topic: "/IMU_data"
  small_update_max_iterations: 1
  bigupdate_max_iterations: 2
  small_update_interval: 0.03
```

launch 中当前使用：

```xml
point_filter_num = 1
filter_size_surf = 0.2
filter_size_map = 0.5
```

解释：

- `point_filter_num=1`：尽量保留 SAT-SLAM 输入给 SAULIO 的点。
- `filter_size_surf=0.2`：增加当前帧可用于匹配的线面观测。
- `filter_size_map=0.5`：控制地图密度和运行时间。

如果 SAULIO 单独运行，需要把：

```yaml
common:
  lid_topic: "/velodyne_points"
```

此时输入变为原始点云，观测特征数量通常会明显多于联合模式。

## 11. 常用话题

| 话题 | 发布方 | 订阅方/用途 |
| --- | --- | --- |
| `/velodyne_points` | rosbag/雷达 | fast_lio_sam、实时检测 |
| `/IMU_data` | rosbag/IMU | fast_lio_sam、SAULIO |
| `/detect3d` | 实时检测节点 | fast_lio_sam |
| `/sat_slam/saulio_filtered_points` | fast_lio_sam | SAULIO |
| `/aft_mapped_to_init` | SAULIO | fast_lio_sam 外部前端模式 |
| `/cloud_registered` | LIO 节点 | RViz |
| `/limot/tracked_objects` | fast_lio_sam | RViz 跟踪目标 |
| `/limot/object_local_maps` | fast_lio_sam | 可选目标局部地图显示 |

## 12. 推荐测试顺序

1. 先用 `frontend_odom_mode=0` 和离线 txt 验证原 FAST-LIO 路线。
2. 设置 `frontend_odom_mode=1`，启动两个包，验证 SAULIO 里程计路线。
3. 保持前端不变，对比 `dynamic_filter_mode=0/1/2`。
4. 对比 `backendResultMode=0/1`。
5. 最后再把离线 txt 切换到实时 TF 或 TRT 检测。

这样可以把前端、检测、动态剔除和后端问题分开定位。
